package main

func main() {
	// takes 2 links as arguments
	// goes to find the raw text of the decklists
	// parse them into 2 arrays
	// compare the 2 arrays
	// lists the items that don't exist in the other
}
